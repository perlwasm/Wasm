use strict;
use warnings;
use Alien::wasmtime;

our $DIR;

{
  # we're just using the header files, not
  # linking against libwasmtime for now.
  cflags => Alien::wasmtime->cflags,
  source => [ "$DIR/*.c" ],
}
